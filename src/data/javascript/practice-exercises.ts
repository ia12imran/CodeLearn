export interface PracticeExercise {
  title: string;
  description: string;
  topic: string;
  level: "Easy" | "Medium" | "Hard";
  starter: string;
  solution: string;
}

export const practiceExercises: PracticeExercise[] = [
  {
    title: "Hello from the Console",
    description: "Print a friendly greeting to the console. Use console.log with single-quoted strings and combine values with the + operator.",
    topic: "Introduction",
    level: "Easy",
    starter: "// TODO: print 'Hello, World!' to the console\n// TODO: print 'I am learning JavaScript'\n// TODO: combine 'Automation ' and 'rocks!' on one line",
    solution: "console.log('Hello, World!');\nconsole.log('I am learning JavaScript');\nconsole.log('Automation ' + 'rocks!');",
  },
  {
    title: "Arithmetic Basics",
    description: "Print the results of simple arithmetic: addition, subtraction, multiplication, division, and the remainder.",
    topic: "Introduction",
    level: "Easy",
    starter: "// TODO: print the result of 12 + 9\n// TODO: print the result of 12 - 9\n// TODO: print the result of 12 * 9\n// TODO: print the remainder of 12 divided by 9",
    solution: "console.log('12 + 9 = ' + (12 + 9));\nconsole.log('12 - 9 = ' + (12 - 9));\nconsole.log('12 * 9 = ' + (12 * 9));\nconsole.log('12 / 9 = ' + (12 / 9));\nconsole.log('12 % 9 = ' + (12 % 9));",
  },
  {
    title: "let vs const",
    description: "Use let for a value you reassign and const for a value that never changes. Remember: const cannot be reassigned.",
    topic: "Variables",
    level: "Easy",
    starter: "let steps = 0;\n// TODO: reassign steps to 3 and log the new value\nconst site = 'example.org';\n// TODO: log the site name",
    solution: "let steps = 0;\nsteps = 3;\nconsole.log('Steps taken: ' + steps);\nconst site = 'example.org';\nconsole.log('Site under test: ' + site);\nconsole.log('typeof steps: ' + typeof steps);",
  },
  {
    title: "Type Detective With typeof",
    description: "Inspect a value's type with typeof and print meaningful labels for numbers, strings, booleans, arrays, and objects.",
    topic: "Variables",
    level: "Easy",
    starter: "const role = 'admin';\n// TODO: log the typeof of role\nconst retries = 3;\n// TODO: log the typeof of retries\nconst isPassed = true;\n// TODO: log the typeof of isPassed\n// TODO: log the typeof of an array [] and an object {}",
    solution: "const role = 'admin';\nconsole.log('role is a ' + typeof role);\nconst retries = 3;\nconsole.log('retries is a ' + typeof retries);\nconst isPassed = true;\nconsole.log('isPassed is a ' + typeof isPassed);\nconsole.log('array typeof: ' + typeof []);\nconsole.log('object typeof: ' + typeof {});\nconsole.log('null typeof: ' + typeof null);",
  },
  {
    title: "Strict Comparisons",
    description: "Compare values with === and !== and report whether assertion-style checks pass. Watch the classic pitfall: the string '10' is not the number 10.",
    topic: "Operators",
    level: "Easy",
    starter: "const apiStatus = 200;\nconst uiStatus = 200;\n// TODO: log whether apiStatus strictly equals uiStatus\nconst textCount = '10';\n// TODO: log whether textCount === 10\n// TODO: convert textCount with Number() and compare again",
    solution: "const apiStatus = 200;\nconst uiStatus = 200;\nconsole.log('Status matches: ' + (apiStatus === uiStatus));\nconst textCount = '10';\nconsole.log('String 10 equals number 10: ' + (textCount === 10));\nconsole.log('Number() conversion matches: ' + (Number(textCount) === 10));\nconsole.log('5 > 3 is ' + (5 > 3));\nconsole.log('10 <= 10 is ' + (10 <= 10));",
  },
  {
    title: "Logical & Ternary",
    description: "Use &&, ||, and the ternary operator to decide test outcomes and pick fallback values when data is missing.",
    topic: "Operators",
    level: "Medium",
    starter: "const isLoggedIn = true;\nconst isAdmin = false;\n// TODO: log if access is granted (needs both conditions)\n// TODO: pick a fallback display name with ||\n// TODO: log PASS or FAIL for a score with a ternary",
    solution: "const isLoggedIn = true;\nconst isAdmin = false;\nconsole.log('Admin access granted: ' + (isLoggedIn && isAdmin));\nconst name = '';\nconst displayName = name || 'guest';\nconsole.log('Display name: ' + displayName);\nconst score = 82;\nconst verdict = score >= 70 ? 'PASS' : 'FAIL';\nconsole.log('Test verdict: ' + verdict);\nconsole.log('Customer tier: ' + (score > 90 ? 'VIP' : 'regular'));",
  },
  {
    title: "Grade the Score",
    description: "Write an if / else if / else chain that maps a score to a grade: 90+ is A, 80+ is B, 70+ is C, 60+ is D, otherwise F.",
    topic: "Control Flow",
    level: "Easy",
    starter: "const score = 85;\nlet grade = '';\n// TODO: use if / else if / else to set the grade\n// TODO: log the final grade",
    solution: "const score = 85;\nlet grade = '';\nif (score >= 90) {\n  grade = 'A';\n} else if (score >= 80) {\n  grade = 'B';\n} else if (score >= 70) {\n  grade = 'C';\n} else if (score >= 60) {\n  grade = 'D';\n} else {\n  grade = 'F';\n}\nconsole.log('Score: ' + score + ' -> grade: ' + grade);",
  },
  {
    title: "Even Numbers & Nested Loops",
    description: "Loop from 1 to 10 and log only the even numbers, then use a nested loop to print a small multiplication table.",
    topic: "Control Flow",
    level: "Medium",
    starter: "// TODO: log every even number from 2 to 10\n// TODO: nested loop with i and j from 1 to 3, log i * j",
    solution: "for (let i = 1; i <= 10; i++) {\n  if (i % 2 === 0) {\n    console.log('Even: ' + i);\n  }\n}\nfor (let i = 1; i <= 3; i++) {\n  for (let j = 1; j <= 3; j++) {\n    console.log(i + ' x ' + j + ' = ' + (i * j));\n  }\n}",
  },
  {
    title: "Split, Join & Search",
    description: "Use split and join to convert between strings and arrays, then verify text with includes and startsWith.",
    topic: "Strings",
    level: "Easy",
    starter: "const csv = 'chrome,firefox,safari';\n// TODO: split csv into an array and log it\n// TODO: join the array back with ' | ' and log it\nconst url = 'https://example.com/login';\n// TODO: log if url includes 'login'\n// TODO: log if url starts with 'https://'",
    solution: "const csv = 'chrome,firefox,safari';\nconst browsers = csv.split(',');\nconsole.log('Browsers: ' + browsers);\nconsole.log('Joined: ' + browsers.join(' | '));\nconst url = 'https://example.com/login';\nconsole.log('Has login: ' + url.includes('login'));\nconsole.log('Secure: ' + url.startsWith('https://'));\nconsole.log('Ends with .com: ' + url.endsWith('.com'));",
  },
  {
    title: "Clean & Pad Text",
    description: "Trim scraped text, replace values, and pad numbers so your test reports line up nicely.",
    topic: "Strings",
    level: "Medium",
    starter: "const raw = '  Total: 42  ';\n// TODO: trim the whitespace and log the clean text\n// TODO: replace 'Total' with 'Sum' and log it\n// TODO: pad the number 7 with zeros to length 3 and log it",
    solution: "const raw = '  Total: 42  ';\nconsole.log('Trimmed: ' + raw.trim());\nconsole.log('Swapped: ' + raw.trim().replace('Total', 'Sum'));\nconst id = '7';\nconsole.log('Padded id: ' + id.padStart(3, '0'));\nconst name = 'CoDe';\nconsole.log('Lower: ' + name.toLowerCase());\nconsole.log('Checks: ' + 'banana'.includes('nan'));",
  },
  {
    title: "Arrow vs Declaration",
    description: "Write the same logic as both a function declaration and an arrow function, then use each one to transform values.",
    topic: "Functions",
    level: "Medium",
    starter: "// TODO: declare function double(n) that returns n * 2\n// TODO: declare arrow const triple = (n) => n * 3\n// TODO: log double(5) and triple(5)",
    solution: "function double(nValue) {\n  return nValue * 2;\n}\nconst triple = (nValue) => nValue * 3;\nconsole.log('double(5): ' + double(5));\nconsole.log('triple(5): ' + triple(5));\nconst names = ['ana', 'bob'];\nconst shout = names.map((n) => n.toUpperCase());\nconsole.log('Shouted: ' + shout);",
  },
  {
    title: "Defaults & Rest",
    description: "Give a parameter a default value and collect extra arguments with a rest parameter, then reduce them to a total.",
    topic: "Functions",
    level: "Medium",
    starter: "// TODO: function greet(name = 'friend') returns 'Hello, Name'\n// TODO: function total(...numbers) returns the sum of all numbers\n// TODO: log greet() and total(5, 10, 15)",
    solution: "function greet(name = 'friend') {\n  return 'Hello, ' + name + '!';\n}\nfunction total(...numbers) {\n  return numbers.reduce((sum, n) => sum + n, 0);\n}\nconsole.log(greet());\nconsole.log(greet('Ana'));\nconsole.log('Total: ' + total(5, 10, 15));\nconsole.log('Total with more: ' + total(1, 2, 3, 4, 5));",
  },
  {
    title: "Map, Filter & Find",
    description: "Filter active users, map names to uppercase, find the first match, and check membership with some and includes.",
    topic: "Arrays",
    level: "Medium",
    starter: "const users = [\n  { name: 'ana', active: true },\n  { name: 'bob', active: false },\n  { name: 'cin', active: true },\n];\n// TODO: log the names of the active users\n// TODO: log the first active user's name\n// TODO: log whether any user is inactive",
    solution: "const users = [\n  { name: 'ana', active: true },\n  { name: 'bob', active: false },\n  { name: 'cin', active: true },\n];\nconst activeNames = users.filter((u) => u.active).map((u) => u.name);\nconsole.log('Active users: ' + activeNames);\nconst firstActive = users.find((u) => u.active);\nconsole.log('First active: ' + firstActive.name);\nconsole.log('Any inactive: ' + users.some((u) => !u.active));\nconsole.log('Has ana: ' + users.map((u) => u.name).includes('ana'));",
  },
  {
    title: "Reduce & Sort",
    description: "Sum a list of prices with reduce, then sort numbers correctly with a comparator instead of the default string sort.",
    topic: "Arrays",
    level: "Medium",
    starter: "const prices = [19.99, 4.5, 12];\n// TODO: reduce prices to a total and log it\nconst nums = [10, 2, 100, 1];\n// TODO: sort nums ascending using a comparator and log it",
    solution: "const prices = [19.99, 4.5, 12];\nconst total = prices.reduce((sum, price) => sum + price, 0);\nconsole.log('Total: ' + total.toFixed(2));\nconst nums = [10, 2, 100, 1];\nconst sorted = [...nums].sort((a, b) => a - b);\nconsole.log('Sorted: ' + sorted);\nconsole.log('Count: ' + prices.length);",
  },
  {
    title: "Explore Object Keys",
    description: "Read fields with dot and bracket notation, then list the keys, values, and entries of an object.",
    topic: "Objects",
    level: "Medium",
    starter: "const settings = { theme: 'dark', lang: 'en', retries: 3 };\n// TODO: log the theme with dot notation\n// TODO: log the lang with bracket notation\n// TODO: log Object.keys, Object.values, and Object.entries",
    solution: "const settings = { theme: 'dark', lang: 'en', retries: 3 };\nconsole.log('Theme: ' + settings.theme);\nconsole.log('Lang: ' + settings['lang']);\nconsole.log('Keys: ' + Object.keys(settings));\nconsole.log('Values: ' + Object.values(settings));\nconsole.log('Entries: ' + Object.entries(settings));\nfor (const [key, value] of Object.entries(settings)) {\n  console.log(key + ' = ' + value);\n}",
  },
  {
    title: "Merge & Optional Chaining",
    description: "Merge two config objects with the spread operator and read a nested field safely with optional chaining plus a fallback.",
    topic: "Objects",
    level: "Medium",
    starter: "const base = { browser: 'chromium', headless: true };\nconst extra = { timeout: 5000, headless: false };\n// TODO: merge base and extra so extra wins and log it\nconst user = { profile: { name: 'Ana' } };\n// TODO: log user.profile.name with dot notation\n// TODO: log a fallback message when user.account?.email is missing",
    solution: "const base = { browser: 'chromium', headless: true };\nconst extra = { timeout: 5000, headless: false };\nconst merged = { ...base, ...extra };\nconsole.log('Merged: ' + JSON.stringify(merged));\nconst user = { profile: { name: 'Ana' } };\nconsole.log('Name: ' + user.profile.name);\nconst email = user.account?.email ?? 'no email';\nconsole.log('Email: ' + email);",
  },
  {
    title: "Promise Chain & async/await",
    description: "Simulate waiting for a page with setTimeout. Chain a Promise with .then, then rewrite the same flow with async/await.",
    topic: "Async",
    level: "Medium",
    starter: "function wait(ms) {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n// TODO: chain wait(200).then() to log 'Page loaded.'\n// TODO: async function loadPage() that awaits wait and logs a line\n// TODO: call loadPage()",
    solution: "function wait(ms) {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\nwait(200).then(() => {\n  console.log('Page loaded.');\n});\nasync function loadPage() {\n  await wait(200);\n  console.log('Async page loaded.');\n}\nloadPage();",
  },
  {
    title: "Promise.all Parallel Waits",
    description: "Run several simulated waits in parallel with Promise.all and print each result combined into a single line of output.",
    topic: "Async",
    level: "Hard",
    starter: "function wait(ms, label) {\n  return new Promise((resolve) => setTimeout(() => resolve(label), ms));\n}\nconst steps = ['open', 'fill', 'submit'];\n// TODO: use Promise.all to await all steps in parallel\n// TODO: log the results joined by ' -> '",
    solution: "function wait(ms, label) {\n  return new Promise((resolve) => setTimeout(() => resolve(label), ms));\n}\nconst steps = ['open', 'fill', 'submit'];\nPromise.all(steps.map((s) => wait(200, s))).then((results) => {\n  console.log('All done in parallel: ' + results.join(' -> '));\n});",
  },
  {
    title: "Class With Constructor & Getter",
    description: "Build a User class with a constructor, a getter, and a static method, then instantiate users and describe them.",
    topic: "Classes",
    level: "Medium",
    starter: "// TODO: class User with a constructor(name, role)\n// TODO: getter label that returns 'name (role)'\n// TODO: static createGuest() that returns a User\n// TODO: log an admin label and a guest label",
    solution: "class User {\n  constructor(name, role) {\n    this.name = name;\n    this.role = role;\n  }\n  get label() {\n    return this.name + ' (' + this.role + ')';\n  }\n  static createGuest() {\n    return new User('guest', 'viewer');\n  }\n}\nconst admin = new User('Ana', 'admin');\nconsole.log('Admin label: ' + admin.label);\nconst guest = User.createGuest();\nconsole.log('Guest label: ' + guest.label);",
  },
  {
    title: "Inheritance With super",
    description: "Create a BaseTest class and a LoginTest subclass that calls super in its constructor and overrides a run method.",
    topic: "Classes",
    level: "Hard",
    starter: "// TODO: class BaseTest with a constructor(name) and a run() that logs\n// TODO: class LoginTest extends BaseTest and calls super(name)\n// TODO: override run() to log the username as well\n// TODO: create a LoginTest and call run()",
    solution: "class BaseTest {\n  constructor(name) {\n    this.name = name;\n  }\n  run() {\n    console.log('Starting: ' + this.name);\n  }\n}\nclass LoginTest extends BaseTest {\n  constructor(name, username) {\n    super(name);\n    this.username = username;\n  }\n  run() {\n    super.run();\n    console.log('Logging in as: ' + this.username);\n  }\n}\nconst login = new LoginTest('LoginFlow', 'tester1');\nlogin.run();",
  },
  {
    title: "Simulated Query by Tag",
    description: "There is no real browser in this sandbox, so query a plain JavaScript array of fake elements. Write helpers that filter by tag name, class, and text.",
    topic: "DOM",
    level: "Hard",
    starter: "const elements = [\n  { tag: 'button', class: 'btn submit', text: 'Submit' },\n  { tag: 'input', class: 'field', text: '' },\n  { tag: 'button', class: 'btn cancel', text: 'Cancel' },\n];\n// TODO: helper byTag(tag) returns matching elements\n// TODO: helper byClass(cls) returns elements whose class includes cls\n// TODO: helper byText(text) returns elements whose text includes text",
    solution: "const elements = [\n  { tag: 'button', class: 'btn submit', text: 'Submit' },\n  { tag: 'input', class: 'field', text: '' },\n  { tag: 'button', class: 'btn cancel', text: 'Cancel' },\n];\nfunction byTag(tag) {\n  return elements.filter((el) => el.tag === tag);\n}\nfunction byClass(cls) {\n  return elements.filter((el) => el.class.includes(cls));\n}\nfunction byText(text) {\n  return elements.filter((el) => el.text.includes(text));\n}\nconsole.log('Buttons: ' + byTag('button').map((el) => el.text));\nconsole.log('Class btn count: ' + byClass('btn').length);\nconsole.log('Submit button: ' + byText('Submit')[0].tag);",
  },
  {
    title: "Combined Query Simulation",
    description: "Combine tag, class, and text filters into one query helper and report what the simulated selector returns.",
    topic: "DOM",
    level: "Hard",
    starter: "const nodes = [\n  { tag: 'a', class: 'nav link', text: 'Home' },\n  { tag: 'a', class: 'nav link', text: 'About' },\n  { tag: 'li', class: 'nav item', text: 'Contact' },\n];\n// TODO: helper queryAll(tag, cls) filters by tag and class\n// TODO: helper hasText(node, text) checks text ignoring case\n// TODO: log the texts matched by queryAll('a', 'link')",
    solution: "const nodes = [\n  { tag: 'a', class: 'nav link', text: 'Home' },\n  { tag: 'a', class: 'nav link', text: 'About' },\n  { tag: 'li', class: 'nav item', text: 'Contact' },\n];\nfunction queryAll(tag, cls) {\n  return nodes.filter((n) => n.tag === tag && n.class.includes(cls));\n}\nfunction hasText(node, text) {\n  return node.text.toLowerCase().includes(text.toLowerCase());\n}\nconsole.log('Nav anchors: ' + queryAll('a', 'link').map((n) => n.text));\nconsole.log('Contains About: ' + queryAll('a', 'link').some((n) => hasText(n, 'about')));",
  },
  {
    title: "Registry Export Pattern",
    description: "Simulate a module system with a registry object: functions that register names to exports and import them back by name.",
    topic: "Modules",
    level: "Hard",
    starter: "// TODO: create a registry object that holds exports\n// TODO: register(name, value) stores a value in the registry\n// TODO: importName(name) returns the value or a 'missing' message\n// TODO: register a function and a string, then import them",
    solution: "const registry = {};\nfunction register(name, value) {\n  registry[name] = value;\n}\nfunction importName(name) {\n  return registry[name] ?? 'missing export: ' + name;\n}\nregister('getUrl', () => 'https://example.com');\nregister('env', 'staging');\nconsole.log('env: ' + importName('env'));\nconsole.log('Call: ' + importName('getUrl')());\nconsole.log('Missing: ' + importName('nope'));",
  },
  {
    title: "Import Builders & Re-exports",
    description: "Simulate default and named imports by building objects with export lists, then merge them into a combined export object.",
    topic: "Modules",
    level: "Hard",
    starter: "// TODO: object namedExports with buildUrl and buildHeader helpers\n// TODO: object defaultExport with a headers() function\n// TODO: merge both into a combined object with spread\n// TODO: log buildUrl, buildHeader, and the merged keys",
    solution: "const namedExports = {\n  buildUrl: (base, path) => base + '/' + path,\n  buildHeader: (token) => 'Bearer ' + token,\n};\nconst defaultExport = {\n  headers: () => ({ auth: 'token' }),\n};\nconst combined = { ...namedExports, ...defaultExport };\nconsole.log('Url: ' + combined.buildUrl('https://api.test.com', 'users'));\nconsole.log('Header: ' + combined.buildHeader('abc123'));\nconsole.log('Headers: ' + JSON.stringify(combined.headers()));\nconsole.log('Merged keys: ' + Object.keys(combined));",
  },
];